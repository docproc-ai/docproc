# General Rules

1. Use your memory bank when appropriate (`memory-bank/`).
2. Do not put status update markdown files in the root directory.
3. Don't create migration scripts to just move things around or migrate to a new version of a library.
